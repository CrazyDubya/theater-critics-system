# theater-critics-system

The name does not match the contents. A full recursive listing of this repository returns two things: `prison/` and `git-organization-strategy.md`. There is no theater critics code here.

## What's actually here

- `prison/` — a multi-agent simulation. Its own `README.md`, `QUICK_START.md` and `PROJECT_STATUS.md` are the real documentation. Includes an `eternal_lockdown/` scenario and an AutoGen integration, plus `EXPERIMENT_ollama-integration.md` on running against local models.
- `git-organization-strategy.md` — the same file also present in `git-managed` and `git-workflow-project`.

## Where to look instead

The theater critics work is in `theater-critics`. A separate `prison-simulation` repository also exists. This repository is 214 MB filed under a name that will send you to the wrong place; it should be renamed or folded into `prison-simulation`.
